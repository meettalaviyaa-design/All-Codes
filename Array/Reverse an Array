class Solution {
    public void reverseArray(int arr[]) {
        
         int last=arr.length-1;
	     for(int i=0;i<arr.length/2;i++){
	            int temp=arr[i];
	            arr[i]=arr[last];
	            arr[last]=temp;
	            last--;
	     }
    }
}
