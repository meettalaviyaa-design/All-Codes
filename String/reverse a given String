public class raam{
	
	public static void main(String[] args) {
	
		String name="hello";
		String copy="";
		
		for(int i=name.length()-1;i>=0;i--) {
			copy+=name.charAt(i);
		}
		name=copy;
		System.out.println(name); 
// Output 
	}
}
