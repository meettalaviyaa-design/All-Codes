int array[]= {1,2,3,4,5,6,7,8};
		int k=10;
		
		int sum=0;
		int count=0;
		int final_max=0;
		for(int i=0,j=0;j<array.length;j++) {
			
			sum=sum+array[j];
			while(sum>k) {
				sum=sum-array[i];
				i++;
			}
			
			count=j-i+1;
			final_max=Math.max(final_max, count); // fpr find maximum
			
		}
		System.out.println(final_max);
		
