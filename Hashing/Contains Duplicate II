int array[]= {1, 2, 3, 1};

		 int k=3;
		 
		 HashMap<Integer, Integer> hs=new HashMap<>();
		 
		 for(int i=0;i<array.length;i++) {
			  if(hs.containsKey(array[i])) {
				 int min= Math.abs((hs.get(array[i]))-i);
				 if(min<=k) {
					 System.out.println("True");
					 break;
				 }
			 }
			 hs.put(array[i], i);
		 }
